%tr.caserow
  %td{width: '80%'}
    %p= link_to post.title, post_path(post)
  %td= link_to 'edit', edit_post_path(post), class: 'intable button'
  %td= button_to 'delete', post_path(post), method: :delete, data: {confirm: 'Are you sure you want to delete this post?'}, class: 'intable alert button'
