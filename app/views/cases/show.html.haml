.row
  .medium-10.columns.medium-centered

    %h2= @case.title

    %p.keywords= "Subjects: " + @case.keyword_string

    = render_markdown(@case.summary)

    %h3 Facts
    = render_markdown(@case.facts)

    %h3 Held
    = render_markdown(@case.held)

    %h3 Comment
    = render_markdown(@case.comment)
