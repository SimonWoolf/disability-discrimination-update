.row
  .medium-10.columns.medium-centered

    %h2= @case.title

    %p.keywords= "Subjects: " + @case.keyword_string

    %p= render_markdown(@case.summary)

    %h3 Facts
    %p= render_markdown(@case.facts)

    %h3 Held
    %p= render_markdown(@case.held)

    %h3 Comment
    %p= render_markdown(@case.comment)
