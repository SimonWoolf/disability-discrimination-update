%h1 Cases by keyword

.row
  .medium-12.small-centered.columns
    %table{width: '100%'}
      %tbody
        - @keywords.each do |keyword|
          %tr
            %td
              %h5= keyword
          = render partial: 'case', collection: @cases_by_keyword[keyword], as: '_case'
